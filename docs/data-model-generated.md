AUTOGENERATED FILE DO NOT EDIT

This file contains autogenerated "schema" for Babbleserv's FoundationDB keyvalues.


# Accounts Database

### AccountDataDirectory

| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `accountDataTupToVersion` | `types.AccountDataTup` | `Versionstamp` | AccountDataTup to version, used to clear previous keys when new ones appended<br/> |
| `userVersionToAccountData` | `(UserID, Versionstamp)` | `types.AccountData` | Per user sparse stream of account data, only contains the latest version of each tup<br/>- get whole range for init sync (all account data)<br/>- paginate range for inc sync<br/> |

### DevicesDirectory

| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `byUserDeviceID` |  |  | UserID/DeviceID to types.Device msgpack bytes<br/> |
| `userDeviceLastSeen` |  |  | UserID/DeviceID to last seen time, separate to device bytes since updated on any req<br/> |
| `syncConns` |  |  | UserID/DeviceID/ConnID to types.UserSyncConn msgpack bytes, stores basic information about<br/>(native/sliding) sync connections which v2/streaming also use. Also acts as a lock preventing<br/>parallel sync calls with the same deviceid/connid. Updated every sync req.<br/> |

### TokensDirectory

| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `authTokens` |  |  |  |

### UsersDirectory

| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `byVersion` |  |  | / version -> username<br/> |
| `byUsername` | `(Username)` | `types.User (as msgpack []byte)` | / username -> *types.User<br/> |
| `userPasswordHashes` |  |  | username -> password hash<br/> |
| `userMasterKeys` |  |  | Device signing keys<br/>https://spec.matrix.org/v1.11/client-server-api/#post_matrixclientv3keysdevice_signingupload<br/>username -> CrossSigningKey JSON (CSAPI)<br/> |
| `userSelfSigningKeys` |  |  | username -> CrossSigningKey JSON (CSAPI)<br/> |
| `userUserSigningKeys` |  |  | username -> CrossSigningKey JSON (CSAPI)<br/> |
| `userFilters` |  |  | Username/version -> filter bytes, version returned to the user as the filter ID. PUT requests<br/>will then update the filter in-place. Meaning filters aren't stored as an immutableish stream.<br/> |


# Media Database

| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `byVersion` |  |  |  |


# Rooms Database

### EventsDirectory

EventsDirectory is where events and associated metadata are stored in addition to a number of
indices to enable fast access to various bits of (room) event data.


| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `idToEvent` | `EventID` | `types.Event (as msgpack []byte)` | Full event bytes by ID (turned into `types.Event`), contians the full event content and extra<br/>metadata like `auth_events` & `prev_events`.<br/> |
| `versionToTup` | `tuple.Versionstamp` | `types.EventTup (as tuple.Tuple []byte)` | Ordered EventTups by version, for paginating all events over all rooms. Used by internal<br/>services for things like profile update propagation.<br/> |
| `idToVersion` | `EventID` | `Versionstamp` | Event ID to `tuple.Versionstamp`. Allows us to get the version of an event so we can lookup state<br/>at that point in time.<br/> |
| `roomVersionToTup` | `(RoomID, tuple.Versionstamp)` | `types.EventTup (as tuple.Tuple []byte)` | Ordered `EventTup`s by room/version, for paginating all events in a room<br/> |
| `localRoomVersionToTup` | `(RoomID, tuple.Versionstamp)` | `types.EventTup (as tuple.Tuple []byte)` | Ordered `EventTup`s originating from this homeserver, for paginating events to send over<br/>federation.<br/> |
| `roomStateVersionToTup` | `(RoomID, tuple.Versionstamp)` | `types.EventStateTup` | Ordered `EventStateTup`s by room/version, for pagination of room state changes.<br/>- pull everything <= by versionstamp, latest (type, state_key) pairs win<br/>- means iterating over duplicates, but alternative is to snapshot state at each change which<br/>  is extremely space wasteful.<br/>- "get state changes between X and Y events in this room"<br/>  (S2S API needs MSC, see [this synapse issue](https://github.com/matrix-org/synapse/issues/13618))<br/> |
| `roomExtremIDs` | `(RoomID, EventID)` | `empty []byte` | Current room extremeties, keys only roomID/eventID. We range over them to pull the current<br/>DAG extremeties before clearing when writing events (and setting the written event).<br/>- on local send take all event IDs as `prev_events`<br/>- on store event clear any found in `prev_events`<br/> |
| `idToLastResolvedData` |  |  | Room ID to tuple of information from the last time state resolution was performed on the room<br/>ie (versionstamp, eventID1, eventID2, ...). We combine this with the list of current extrem<br/>IDs to identify whether we need to re-resolve the room again (if multiple forks have written<br/>state changes since last resolve).<br/> |
| `roomVersionToIDStateTup` | `(RoomID, EventType, StateKey, Versionstamp)` | `EventID` | State events by room/type/version, for paginating the history of a single state event. This<br/>allows us to fetch state for a given room/type at a point in time, for authorizing events.<br/>- "get m.room.power_levels at version X"<br/> |
| `currentRoomStateToID` | `(RoomID, EventType, StateKey)` | `EventID` | Current state event by room/type for quick lookups and event auth. Also allows fetching the<br/>entire room state quickly (without members) as all the keys are stored together.<br/> |
| `currentRoomMemberships` | `(RoomID, EventType, StateKey)` | `types.MembershipTup` | Current memberships by room/user, quick lookups of specific user memberships in a room and<br/>quick pagination of all members in a room by keeping the keys together.<br/> |
| `currentRoomServers` | `(RoomID, EventType, StateKey)` | `types.MembershipTup` | Current servers by room, used to ensure we're running federation senders for each one when<br/>the room receives updates.<br/> |
| `byRoomRelation` |  |  | RoomID/related-event-ID/version to (eventID, relType)<br/>- paginate events relating to this event<br/>- paginate events of a certain rel_type relating to this event<br/>  - have to paginate through types that don't match<br/>  - probably sufficient performance (rare)<br/> |
| `byRoomReaction` |  |  | Room event reactions<br/>- de-dupe reactions to an event by user/key<br/>- paginate reactions for a given event (`/relations/rel_to_ev_id/m.annotation`)<br/> |
| `roomThreadVersionToID` |  |  | root event ID by room/root-ev-version<br/>Note: only set for new thread roots, on the first replying (in-thread) event.<br/>- paginate thread roots in a room<br/> |

| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `byID` |  |  |  |

### ReceiptsDirectory

| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `receiptTupToVersion` | `(UserID, RoomID, Type, ThreadID)` | `version` | ReceiptTup (user, room, thread, type) to version, used to clear previous keys when new ones are appended.<br/> |
| `roomVersionToReceipt` | `(RoomID, version)` | `types.Receipt (as tuple.Tuple []byte)` | Sparse stream of public receipts stored by room/version for pagination<br/> |
| `localRoomVersionToReceipt` | `(RoomID, version)` | `types.Receipt (as tuple.Tuple []byte)` | Sparse stream of public receipts sent by local users, stored by room/version<br/> |
| `userVersionToReceipt` | `(UserID, version)` | `types.Receipt (as tuple.Tuple []byte)` | Sparse stream of private receipts send by local users, stored by user/version<br/> |

| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `idToRoom` |  |  | Room ID to bytes we decode to `*types.Room` items<br/> |
| `idToDepth` |  |  | Room ID to depth int64<br/> |
| `idToVersion` |  |  | Room ID to tuple.Versionstamp of the most recent receipt or event in the room<br/> |
| `aliasToID` |  |  | Aliases to room IDs<br/> |

### ServersDirectory

| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `joinedRoomMembers` | `(RoomID, ServerName, UserID)` | `[]byte (always empty)` | UserIDs by room/server so we can easily detect when a server goes in/out of a room<br/> |
| `memberships` | `(ServerName, RoomID)` | `[]byte (always empty)` | Room memberships by server so we list (joined) rooms for a given server<br/> |
| `membershipChanges` | `(ServerName, Versionstamp)` | `types.MembershipTup` | Room membership changes by server so we can handle changes during sync (for federation)<br/> |

### UsersDirectory

| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `profiles` |  |  |  |
| `deviceConnRoomPositions` |  |  | UserID/DeviceID/ConnID/RoomID to rooms versions (events-vstamp, receipts-vstamp)<br/> |


# System Database

### ServersDirectory

| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `syncPositions` | `(ServerName)` | `types.VersionMap (as msgpack []byte)` | Server name to sync positions, similar to user devices<br/> |

| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `locks` |  |  | Prefix for distributed locks (util/lock)<br/> |
| `iteratorPositions` |  |  | Prefix for worker iterator positions<br/> |


# Transient Database

### ToDeviceDirectory

The to-device directory holds versioned to-device events stored by user/device or server for
pagination during sync or federation sending. We also use these to store and sync/federate device
list changes (m.device_list_update EDU) and signing key updates (m.signing_key_update EDU).


| Field | Key | Value | Notes |
| --- | --- | --- | --- |
| `localUserMessages` | `(UserID, DeviceID, Version)` | `types.ToDevice` | To-device messages for local user devices<br/> |
| `localUserMessageTxns` | `(UserID, DeviceID, TransactionID)` | `[]byte (always empty)` | Used to-device transaction IDs<br/> |
| `remoteServerMessages` | `(ServerName, version)` | `types.ToDevice (as msgpack []byte)` | To-device messages for other user devices on other homeservers<br/> |

